import { View, Text } from "react-native";

export default function Index() {
  return (
    <View className="flex-1 items-center justify-center bg-gray-100">
      <Text className="text-2xl font-bold text-blue-600">RecallApp Ready</Text>
      <Text className="mt-4 text-gray-600">Tailwind + NativeWind v4 + Expo Router</Text>
    </View>
  );
}
